import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '101-todo-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
